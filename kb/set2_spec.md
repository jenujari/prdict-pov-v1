# Set 2 — the PCA(0.95)-reduced feature set

Generated by `scripts/build_set2_spec.py`. Do not hand-edit — rerun the script.

Resolves [#13](https://github.com/jenujari/prdict-pov-v1/issues/13) (map decision 9). Set 2 is **independent** of set 1 — a parallel PCA treatment of the same numeric block, not a rotation of set 1's already-selected survivors (`CONTEXT.md`; [#15](https://github.com/jenujari/prdict-pov-v1/issues/15)'s own PCA prediction was computed against this same full block for that reason).

## Parameters

**Input.** PCA fits on the `linear_numeric` block (107 columns), fold — same StandardScaler + rows as #11 (fold_fit_rows).

**Bypass.** `cyclic` (92 columns) rides through untouched — rotating a sin/cos pair breaks the unit-circle identity #7 built. Categorical/boolean columns follow #15's per-model policy unchanged; the same columns ride in set 1. Only `linear_numeric` is replaced by components.

**Component count — fixed at 48.** Target variance 0.95, fold 1 only (earliest chronological block). The width is fixed once, from the first fold's own training rows only — no later fold's data ever informs it. Every fold (and the final holdout refit) then fits its own scaler and its own PCA loadings at that fixed width, strictly on its own rows. This is a structural choice (an architecture-defining integer), not a leak of validation statistics — the same distinction #9 draws between the fold-scoped scaler and the once-fit categorical encoders.

**Scaling.** fold-scoped StandardScaler (#9/#11), fit before PCA in the same fold.

**Consumers — both models, one by design as a negative control.**

- **TFT**: primary intended use — dense components suit a smooth encoder.
- **XGBoost**: deliberate negative control — axis-aligned splits and native categorical handling lose meaning on rotated components; if set 2 underperforms set 1 here, that confirms the expectation rather than indicating a bug.

## Per-timestep consistency (item 1)

Probed session at position 2221: transforming it as part of the full matrix and transforming it alone agree (`matches_isolated_transform=True`). The fitted transform is a stateless per-row map, so the same PCA output applies whether a session is read as an encoder-block row or a decoder-block row of any origin's window.

## Per-fold fit

Every block refits its own scaler and PCA loadings on its own training rows, at the fixed width of 48 components:

| Block | Train rows | Components | Explained variance |
|-------|-----------|------------|---------------------|
| fold 1 | 2529 | 48 | 0.95005 |
| fold 2 | 2529 | 48 | 0.94308 |
| fold 3 | 2529 | 48 | 0.94485 |
| fold 4 | 2529 | 48 | 0.9467 |
| fold 5 | 2529 | 48 | 0.94768 |
| final_train (holdout refit) | 2529 | 48 | 0.94845 |

Fold 1's own achieved variance is 0.95005 (it defined the width, so it lands closest to the 0.95 target); later folds see more history and drift slightly around that same width — expected, since a fixed integer width cannot track a growing training set's variance exactly.

For context, [#15](https://github.com/jenujari/prdict-pov-v1/issues/15) predicted **62/107** from a PCA fit on the *entire* history at once; fold 1 alone (a ~10-year training subset, not 26 years) is expected to differ, and does — see the table above for the actual value this pipeline uses.

## Loadings (item 6, fold 1)

Full component x feature loadings for every block are in `kb/set2_spec.json`; here is the top-loading feature per component for fold 1, as a representative sample.

| Component | Top feature | Loading |
|-----------|-------------|---------|
| pca_1 | `sun_saturn_dist` | +0.237 |
| pca_2 | `jupiter_saturn_dist` | +0.297 |
| pca_3 | `sun_ketu_dist` | +0.301 |
| pca_4 | `saturn_speed_long` | +0.271 |
| pca_5 | `mars_distance` | +0.351 |
| pca_6 | `rahu_kshetra_bala` | +0.401 |
| pca_7 | `moon_ketu_dist` | +0.454 |
| pca_8 | `jupiter_latitude` | +0.350 |
| pca_9 | `mercury_speed_long` | +0.530 |
| pca_10 | `saturn_speed_lat` | +0.442 |
| pca_11 | `sun_latitude` | +0.338 |
| pca_12 | `moon_distance` | +0.364 |
| pca_13 | `mars_uchcha_bala` | +0.389 |
| pca_14 | `rahu_navamsha_bala` | +0.639 |
| pca_15 | `mars_ketu_dist` | +0.453 |
| ... | (33 more components) | |
