# Angular columns — periods and the sin/cos transform

Generated by `scripts/build_angular_spec.py`. Do not hand-edit — rerun the script.

Resolves [#7](https://github.com/jenujari/prdict-pov-v1/issues/7). [#3](https://github.com/jenujari/prdict-pov-v1/issues/3) fixed *membership* (53 angular columns); this spec fixes the *transform*.

## The headline: `*_dist` is not an angle

All 34 cross-planet `<a>_<b>_dist` columns hold `abs(lon_a - lon_b)` — a plain absolute difference with **no modular reduction**. Verified exactly, on every row: worst deviation from `abs(lon_a - lon_b)` is `1.05e-12` degrees, against `180.0` for a signed separation and `357.8` for a folded one.

So the column is neither of the two things the ticket asked us to choose between. It is broken as an angle: two planets at 359 and 1 degrees are recorded 358 degrees apart. Across the file, **2,578 cells** record a separation above 180 degrees where the true angular separation is under 30.

Applying sin/cos to the column as given would half-work and half-poison: `cos` is even, so `cos(abs(d)) == cos(d)` and comes out correct by luck, but `sin(abs(d)) == sign(d) * sin(d)`, and that sign tracks which planet holds the larger raw 0-360 number — an artifact of where the coordinate origin sits, not a fact about the sky.

**The separation is therefore recomputed from the longitudes** as a signed quantity, `delta_ab = (lon_b - lon_a) mod 360`, and the file's `_dist` values are discarded. Signed rather than folded because Vedic drishti is directional — Mars aspects the 4th, 7th and 8th houses forward, Jupiter the 5th, 7th and 9th, Saturn the 3rd, 7th and 10th — so applying and separating are different states. `cos(delta)` carries aspect strength (conjunction `+1`, square `0`, opposition `-1`); `sin(delta)` carries the side.

## Transform

```
theta = 2 * pi * (value - origin) / period
emit    <stem>_sin = sin(theta)
        <stem>_cos = cos(theta)
```

The **raw angle does not survive** — sin/cos replaces it. A TFT reads a raw angle through linear layers, which put 359 and 1 at opposite ends of the range; and the threshold handle a raw angle would give XGBoost is already supplied by the `*_sign`, `*_nakshatra_name` and `*_navamsa_sign` categoricals, which are exactly the sector encodings such a split would reconstruct. (Note the assumption in the ticket that keeping both would feed the stage-1 prune a guaranteed pair does **not** hold: for a uniform angle `corr(theta, cos theta)` is 0 and `corr(theta, sin theta)` about `-0.78`, both under the 0.95 threshold.)

| Family | Sources | Period | Origin | Value |
|--------|---------|--------|--------|-------|
| `tithy` | 1 | 30 | 1 | column as given |
| `longitude` | 8 | 360 | 0 | column as given |
| `pada` | 9 | 4 | 1 | column as given |
| `separation` | 27 | 360 | 0 | signed separation, recomputed |
| `weekday` | 1 | 7 | 0 | column as given |

**46 transforms, 92 emitted columns.**

### Periods, column by column

| Source | Stem | Period | Emits |
|--------|------|--------|-------|
| `tithy` | `tithy` | 30 | `tithy_sin`, `tithy_cos` |
| `sun_longitude` | `sun_longitude` | 360 | `sun_longitude_sin`, `sun_longitude_cos` |
| `sun_nakshatra_pada` | `sun_nakshatra_pada` | 4 | `sun_nakshatra_pada_sin`, `sun_nakshatra_pada_cos` |
| `moon_longitude` | `moon_longitude` | 360 | `moon_longitude_sin`, `moon_longitude_cos` |
| `moon_nakshatra_pada` | `moon_nakshatra_pada` | 4 | `moon_nakshatra_pada_sin`, `moon_nakshatra_pada_cos` |
| `saturn_longitude` | `saturn_longitude` | 360 | `saturn_longitude_sin`, `saturn_longitude_cos` |
| `saturn_nakshatra_pada` | `saturn_nakshatra_pada` | 4 | `saturn_nakshatra_pada_sin`, `saturn_nakshatra_pada_cos` |
| `venus_longitude` | `venus_longitude` | 360 | `venus_longitude_sin`, `venus_longitude_cos` |
| `venus_nakshatra_pada` | `venus_nakshatra_pada` | 4 | `venus_nakshatra_pada_sin`, `venus_nakshatra_pada_cos` |
| `mercury_longitude` | `mercury_longitude` | 360 | `mercury_longitude_sin`, `mercury_longitude_cos` |
| `mercury_nakshatra_pada` | `mercury_nakshatra_pada` | 4 | `mercury_nakshatra_pada_sin`, `mercury_nakshatra_pada_cos` |
| `jupiter_longitude` | `jupiter_longitude` | 360 | `jupiter_longitude_sin`, `jupiter_longitude_cos` |
| `jupiter_nakshatra_pada` | `jupiter_nakshatra_pada` | 4 | `jupiter_nakshatra_pada_sin`, `jupiter_nakshatra_pada_cos` |
| `rahu_longitude` | `rahu_longitude` | 360 | `rahu_longitude_sin`, `rahu_longitude_cos` |
| `rahu_nakshatra_pada` | `rahu_nakshatra_pada` | 4 | `rahu_nakshatra_pada_sin`, `rahu_nakshatra_pada_cos` |
| `ketu_nakshatra_pada` | `ketu_nakshatra_pada` | 4 | `ketu_nakshatra_pada_sin`, `ketu_nakshatra_pada_cos` |
| `mars_longitude` | `mars_longitude` | 360 | `mars_longitude_sin`, `mars_longitude_cos` |
| `mars_nakshatra_pada` | `mars_nakshatra_pada` | 4 | `mars_nakshatra_pada_sin`, `mars_nakshatra_pada_cos` |
| `sun_mars_dist` | `sun_mars_sep` | 360 | `sun_mars_sep_sin`, `sun_mars_sep_cos` |
| `sun_mercury_dist` | `sun_mercury_sep` | 360 | `sun_mercury_sep_sin`, `sun_mercury_sep_cos` |
| `sun_jupiter_dist` | `sun_jupiter_sep` | 360 | `sun_jupiter_sep_sin`, `sun_jupiter_sep_cos` |
| `sun_venus_dist` | `sun_venus_sep` | 360 | `sun_venus_sep_sin`, `sun_venus_sep_cos` |
| `sun_saturn_dist` | `sun_saturn_sep` | 360 | `sun_saturn_sep_sin`, `sun_saturn_sep_cos` |
| `sun_rahu_dist` | `sun_rahu_sep` | 360 | `sun_rahu_sep_sin`, `sun_rahu_sep_cos` |
| `moon_mars_dist` | `moon_mars_sep` | 360 | `moon_mars_sep_sin`, `moon_mars_sep_cos` |
| `moon_mercury_dist` | `moon_mercury_sep` | 360 | `moon_mercury_sep_sin`, `moon_mercury_sep_cos` |
| `moon_jupiter_dist` | `moon_jupiter_sep` | 360 | `moon_jupiter_sep_sin`, `moon_jupiter_sep_cos` |
| `moon_venus_dist` | `moon_venus_sep` | 360 | `moon_venus_sep_sin`, `moon_venus_sep_cos` |
| `moon_saturn_dist` | `moon_saturn_sep` | 360 | `moon_saturn_sep_sin`, `moon_saturn_sep_cos` |
| `moon_rahu_dist` | `moon_rahu_sep` | 360 | `moon_rahu_sep_sin`, `moon_rahu_sep_cos` |
| `mars_mercury_dist` | `mars_mercury_sep` | 360 | `mars_mercury_sep_sin`, `mars_mercury_sep_cos` |
| `mars_jupiter_dist` | `mars_jupiter_sep` | 360 | `mars_jupiter_sep_sin`, `mars_jupiter_sep_cos` |
| `mars_venus_dist` | `mars_venus_sep` | 360 | `mars_venus_sep_sin`, `mars_venus_sep_cos` |
| `mars_saturn_dist` | `mars_saturn_sep` | 360 | `mars_saturn_sep_sin`, `mars_saturn_sep_cos` |
| `mars_rahu_dist` | `mars_rahu_sep` | 360 | `mars_rahu_sep_sin`, `mars_rahu_sep_cos` |
| `mercury_jupiter_dist` | `mercury_jupiter_sep` | 360 | `mercury_jupiter_sep_sin`, `mercury_jupiter_sep_cos` |
| `mercury_venus_dist` | `mercury_venus_sep` | 360 | `mercury_venus_sep_sin`, `mercury_venus_sep_cos` |
| `mercury_saturn_dist` | `mercury_saturn_sep` | 360 | `mercury_saturn_sep_sin`, `mercury_saturn_sep_cos` |
| `mercury_rahu_dist` | `mercury_rahu_sep` | 360 | `mercury_rahu_sep_sin`, `mercury_rahu_sep_cos` |
| `jupiter_venus_dist` | `jupiter_venus_sep` | 360 | `jupiter_venus_sep_sin`, `jupiter_venus_sep_cos` |
| `jupiter_saturn_dist` | `jupiter_saturn_sep` | 360 | `jupiter_saturn_sep_sin`, `jupiter_saturn_sep_cos` |
| `jupiter_rahu_dist` | `jupiter_rahu_sep` | 360 | `jupiter_rahu_sep_sin`, `jupiter_rahu_sep_cos` |
| `venus_saturn_dist` | `venus_saturn_sep` | 360 | `venus_saturn_sep_sin`, `venus_saturn_sep_cos` |
| `venus_rahu_dist` | `venus_rahu_sep` | 360 | `venus_rahu_sep_sin`, `venus_rahu_sep_cos` |
| `saturn_rahu_dist` | `saturn_rahu_sep` | 360 | `saturn_rahu_sep_sin`, `saturn_rahu_sep_cos` |
| `weekday` | `weekday` | 7 | `weekday_sin`, `weekday_cos` |

## Dropped as structurally redundant

8 columns. `ketu_longitude` is `rahu_longitude` + 180 on every row (offsets observed: [180.0]), because Ketu *is* the south lunar node. Under the circular encoding that makes each Ketu sin/cos the exact negation of Rahu's — measured correlation `-1.000000` on both members — so they are dropped here rather than left for the fold-level prune to rediscover. The relation is definitional, not sampled, so dropping globally cannot leak; this is the same licence [#3](https://github.com/jenujari/prdict-pov-v1/issues/3) used for the 20 constants. `rahu_ketu_dist` was already dropped there as constant.

- `ketu_longitude`
- `sun_ketu_dist`
- `moon_ketu_dist`
- `mars_ketu_dist`
- `mercury_ketu_dist`
- `jupiter_ketu_dist`
- `venus_ketu_dist`
- `saturn_ketu_dist`

**`ketu_nakshatra_pada` survives**, and the asymmetry is deliberate. 180 degrees is 13.5 nakshatras only under a *uniform* 27-fold scheme; this source is 28-fold with unequal spans, so the antipodal relation does not carry through to pada. Measured: rahu pada does **not** determine ketu pada, and the sin/cos correlations are `-0.925` and `-0.949` — both inside the 0.95 stage-1 threshold, so the fold-level prune would not remove it either.

## Per-column notes

### `tithy` — period 30, origin 1

Exactly `floor(((moon_longitude - sun_longitude) mod 360) / 12) + 1`, verified on every row, ranging 1..30. **Not** off by one: an elongation of 0.004 degrees yields tithy 1, so origin 1 is right and `(tithy - 1) / 30` is the position on the circle.

Note the file carries **no `sun_moon_dist` column** — tithy is the only Sun-Moon separation feature there is, at 12-degree resolution. The continuous elongation is deliberately *not* added as a 28th separation pair: tithi is a named unit of Vedic practice with per-tithi qualities, and the hypothesis under test is about the tradition's own quantities.

### `*_nakshatra_pada` — period 4, origin 1

Genuinely cyclic: ordered by increasing longitude, the Sun's pada wraps 4 -> 1 27 times. Period 4, origin 1.

The ticket asked whether the real cyclic quantity is `(nakshatra index * 4 + pada)` on a 108-fold circle. **It is not.** The source uses the 28-fold scheme *including* Abhijit, with unequal spans — narrowest 4.17 degrees, widest 13.325. That is 112 unequal cells, not a uniform 108-fold circle, and its uniform counterpart is just the longitude, which is already a feature.

### `weekday` — period 7, origin 0, categorical *retained*

The only transform that does not replace its source: `weekday` stays a categorical (per [#3](https://github.com/jenujari/prdict-pov-v1/issues/3)) and additionally gets a cyclic view. Level names line up exactly with the Monday-first `record_date` index, so the position is read off the string.

Sessions per level on the trading-day index:

| Level | Sessions |
|-------|----------|
| `Monday` | 1,309 |
| `Tuesday` | 1,317 |
| `Wednesday` | 1,306 |
| `Thursday` | 1,312 |
| `Friday` | 1,271 |
| `Saturday` | 1 |
| `Sunday` | 1 |

Effective cardinality is 5. The two weekend entries are real special sessions — the Budget Saturday and the Muhurat Sunday — not data errors. Be aware the period-7 circle is a *calendar* fact, not a trading-index one: Friday and Monday are consecutive sessions but sit 3 apart on the circle. The categorical is what carries the discrete weekday effect; the cyclic pair is the smooth calendar position alongside it.

### `*_latitude` — not angular

Confirmed excluded. Latitudes occupy a narrow band around zero (widest is Venus at about +/-8.6 degrees) and never wrap, so they stay linear numeric in [#3](https://github.com/jenujari/prdict-pov-v1/issues/3).

## Pairs are atomic

Feature selection (#12) and PCA (#13) must keep or drop a (sin, cos) pair together, scoring it by the better of its two members. A lone half is not a weaker encoding of the angle but a wrong one: sin alone identifies theta with 180 - theta, cos alone identifies theta with -theta.

A pair is identified purely by name: `<stem>_sin` and `<stem>_cos`. `prdict.angles.pair_groups()` returns the grouping, and [#12](https://github.com/jenujari/prdict-pov-v1/issues/12) and [#13](https://github.com/jenujari/prdict-pov-v1/issues/13) are bound by it.

## Counts

| | |
|---|---|
| Angular columns from #3 | 53 |
| Dropped as redundant (Ketu) | 8 |
| Angular columns transformed | 45 |
| Extra cyclic views (`weekday`) | 1 |
| **Emitted sin/cos columns** | **92** |
| Features before | 215 |
| Features after | 207 |
| **Model input width** | **254** |

Model input width is 14 boolean + 67 categorical + 81 linear numeric + 92 emitted cyclic.

## Downstream

- [#9](https://github.com/jenujari/prdict-pov-v1/issues/9) — sin/cos output is already bounded to `[-1, 1]`; decide whether it is scaled again with the linear block or passed through.
- [#12](https://github.com/jenujari/prdict-pov-v1/issues/12) and [#13](https://github.com/jenujari/prdict-pov-v1/issues/13) — bound by pair atomicity above.
- [#15](https://github.com/jenujari/prdict-pov-v1/issues/15) — every separation, tithy, pada, nakshatra, sign and navamsa column is an exact function of the 9 longitudes. The Ketu drop here removes only the provable duplicates *among angular columns*; the wider functional-dependency question is that ticket's.

  Found in passing and handed over rather than acted on: the antipodal relation also makes `ketu_sign`, `ketu_navamsa_sign` exact functions of their Rahu counterparts, because signs (30 degrees) and navamsas (30/9) are uniform divisions and 180 is a whole number of each. Those are **categoricals**, so removing them is #9's and #15's call, not this ticket's. `ketu_nakshatra_name` is *not* in that set — Abhijit again.
